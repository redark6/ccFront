import { BillingInformations } from './billing-informations';

describe('BillingInformations', () => {
  it('should create an instance', () => {
    expect(new BillingInformations()).toBeTruthy();
  });
});
